var format = {
  toFixed2: function(num) {
    // 確保輸入是數字，並提供預設值 0
    num = parseFloat(num);
    if (isNaN(num)) {
      num = 0;
    }
    return num.toFixed(2);
  }
}

module.exports = {
  toFixed2: format.toFixed2
}